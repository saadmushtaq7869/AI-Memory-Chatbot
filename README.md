# AI Chat Memory + FastAPI

This project is a simple AI chatbot with:

- Transformer model (GPT-2)
- Memory using LangChain
- FastAPI backend
- Simple agent (chat + calculator)

## Features
- Chat with memory
- Context-aware responses
- Basic calculation support

## Run Project

```bash
uvicorn main:app --reload
